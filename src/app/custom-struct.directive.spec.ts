import { CustomStructDirective } from './custom-struct.directive';

describe('CustomStructDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStructDirective();
    expect(directive).toBeTruthy();
  });
});
